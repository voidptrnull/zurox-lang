# Pygments Zurox

This is used for integrating zurox with the pygments ecosystem.

# Building

In the current directory (i.e. directory containing this file), run
```sh
pip install .
```

# Integration

MkDocs and pygments should automatically handle that.
Check `mkdocs.yml` file for information and configuration regarding it.